// the module
import {OpenstadReactAdmin} from './App.jsx';
export default OpenstadReactAdmin;

/*
// render the base element
function renderElement( elem, config ) {
  let attributes = elem.attributes;
	ReactDOM.render( <OpenstadReactAdmin attributes={attributes} config={config}/>, elem)
}
*/
//export {renderElement}
